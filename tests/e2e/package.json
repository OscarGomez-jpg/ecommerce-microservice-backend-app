{
  "name": "ecommerce-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for ecommerce microservices",
  "scripts": {
    "test:e2e": "cypress run",
    "test:e2e:open": "cypress open",
    "test:e2e:headless": "cypress run --headless --browser chrome",
    "test:jest": "jest --config=jest.config.js",
    "clean:reports": "rm -rf cypress/reports cypress/videos cypress/screenshots"
  },
  "dependencies": {
    "cypress": "^13.6.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "devDependencies": {
    "axios": "^1.6.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0"
  }
}
